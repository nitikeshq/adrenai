# Representative After State

This fixture summarizes the repository after the launch demo runs:

- application source remains unchanged
- `AGENTS.md` provides portable shared guidance
- native Claude Code, Copilot, Cursor, and Kiro files are generated
- `adrenai.yaml` records the selected profile and resolved pack selection
- `.adrenai/generated.json` tracks managed-file ownership and drift

Run `../../scripts/run-demo.ps1` to generate the exact after-state using the
current CLI.
